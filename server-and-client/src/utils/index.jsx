// This code suppose to run inside the sercer component and not inside the client component

// import "server-only";
import "client-only";

export const add = (a, b) => a + b;
export const sub = (a, b) => a - b;
